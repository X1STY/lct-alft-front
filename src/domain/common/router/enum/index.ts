enum ERouterPath {
  SESSIONS = '/sessions',
  TEST_MANY_IMAGES = 'test-many-images',
  SESSION = '/session/$id',
}

enum EIdentifyRoutePath {
  MODULE_PAGE_ROUTE = '/module-page-route',
}

export { ERouterPath, EIdentifyRoutePath }
